name: Auto-Sync & Build Protected Extensions

# ⏰ Trigger setiap hari jam 3 pagi (WIB)
# Atau manual via tab Actions
on:
  schedule:
    - cron: '0 20 * * *' # 3 AM WIB (UTC+7) = 20:00 UTC
  workflow_dispatch:

jobs:
  sync-inject-build:
    name: Sync, Protect, and Build
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout Repo INI (yang isinya script injector)
      - name: Checkout Tools Repo
        uses: actions/checkout@v3
        with:
          path: tools

      # 2. Checkout Repo SUMBER (yang isinya kode asli)
      - name: Checkout Source Repo (duro92)
        uses: actions/checkout@v3
        with:
          repository: duro92/ExtCloud
          path: source

      # 3. Setup Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 4. Inject Proteksi (Jalanin Script)
      - name: 💉 Inject License Checks
        run: |
          echo "Copying injector..."
          cp tools/injector.py source/
          
          echo "Running injection..."
          cd source
          python injector.py
          
          echo "✅ Injection Done. Modified files:"
          git status

      # 5. Setup Java (buat build Gradle)
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      # 6. Build jadi .cs3
      - name: Build Protected Plugins
        run: |
          cd source
          chmod +x gradlew
          ./gradlew make
      
      # 7. Upload Hasil Build
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Protected-Extensions
          path: "source/**/*.cs3"
